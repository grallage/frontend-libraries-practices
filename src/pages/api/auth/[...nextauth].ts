// import nextAuth, { authOptions as defaultAuthOptions } from './demo2'
import nextAuth, { authOptions as defaultAuthOptions } from './demo4'

export const authOptions = defaultAuthOptions
export default nextAuth
